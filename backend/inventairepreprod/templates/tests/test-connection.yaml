apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inventairepreprod.fullname" . }}-test-connection"
  labels:
    {{- include "inventairepreprod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inventairepreprod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
